"use strict";

/**
 * The `Dispatcher` mediates information flow throughout the application. For
 * more information, google Facebook's Flux pattern.
 */
var Dispatcher = require('flux').Dispatcher;

module.exports = new Dispatcher();
