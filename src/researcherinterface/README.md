Before deploying, set the following enviroment variables for the
running user:

- `$DBUSER`: username for database user 
- `$DBPASSWORD`: password for database user
- `$DBHOST`: hostname pointing at database
- `$DBNAME`: name of database
